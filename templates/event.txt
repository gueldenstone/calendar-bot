Events on {{ today }}:
{{range .Events}}
  - {{ if not .IsAllDay }}{{.StartTime}} - {{.EndTime}}: {{ end }}{{.Summary}}{{ if ne .Location `` }} @{{.Location}}{{ end }}
  {{ if ne .TxtDescription `` }}
  {{.TxtDescription}}
  {{end}}
{{end}}
